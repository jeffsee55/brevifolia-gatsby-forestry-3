---
label: List Block
hide_body: true
fields:
- name: blog_posts
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: blog-posts
  label: Blog Posts
